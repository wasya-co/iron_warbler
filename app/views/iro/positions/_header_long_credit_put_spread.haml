
- collapse_key ||= pos.id
- q = pos.quantity

%i.fa.fa-expand.collapse-expand.floaty-collapse{ id: "gameui-pos-detail-#{collapse_key}" }
.maxwidth.positions--header.--header-long-credit-put-spread
  %ul
    %li
      <b>begin $:</b> [#{pp_amount pos.outer.begin_price} << #{pp_amount pos.inner.begin_price}]
      <b>D:</b> [[#{pos.outer.begin_delta} << #{pos.inner.begin_delta}]]
      <b>net_ea/max_gain_ea:</b> #{pp_amount pos.net_amount * 100}/#{pp_amount pos.max_gain * 100} :: <i>#{pp_percent pos.net_percent}</i>
    %li
      <b>end $:</b> [#{pp_amount pos.outer.end_price} << #{pp_amount pos.inner.end_price}]
      <b>D:</b> [[#{pos.outer.end_delta} << #{pos.inner.end_delta}]]
      <b>net*q/max_gain*q:</b> #{pp_amount pos.net_amount * 100*q}/#{pp_amount pos.max_gain * 100*q}
    -# %li
    -#   <b>breakeven:</b> #{pp_amount pos.breakeven}
    -#   <b>max_loss:</b> #{pp_amount pos.max_loss * 100*q}


