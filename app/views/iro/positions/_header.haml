
.positions--header.maxwidth
  = pos
  = link_to '[roll]', roll_position_path(pos) if pos.persisted?
  = link_to '[~]',    edit_position_path(pos) if pos.persisted?
  -# = render "/iro/positions/header_#{pos.strategy.kind}", pos: pos
