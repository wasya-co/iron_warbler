
.datapoints-index.maxwidth
  .header
    %h3.title= @symbol

  %table.bordered.padded
    %thead
      %th quote_at
      %th value
    %tbody
      - @datapoints.each do |point|
        %tr
          %td= pp_datetime point.quote_at
          %td= point.value